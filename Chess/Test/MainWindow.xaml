<Window x:Class="Test.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lin="http://lin/2012/xaml/presentation"
        Title="MainWindow" Height="650" Width="525">
    <Grid>
        <lin:BattleChessView x:Name="chess"/>

        <!--<TextBlock FontSize="40" x:Name="text" Foreground="#a8bb0000" Text="184041008@qq.com" VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False"/>-->
        <!--<lin:ChessPieceView/>-->
        <!--<lin:CheckerboardView/>-->
        <!--<lin:CheckerboardView/>-->
        <!--<Button CommandParameter="{Binding}" Command="{Binding command}" Content="test" VerticalAlignment="Top" HorizontalAlignment="Left"/>
        
        <TextBox Text="{Binding Text,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="0,42,0,199"/>

        <TextBlock Text="{Binding Text}" Margin="0,411,0,0"/>
        <TextBlock Text="{Binding Name}" Margin="0,266,0,0"/>-->
    </Grid>
</Window>
